[phases.setup]
nixPkgs = ["nodejs_22"]
cmds = ["corepack enable"]

[phases.install]
cmds = ["rm -f package-lock.json", "npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npx serve dist -s -l ${PORT:-3000}"
